extends layout

block content
  form(action="/admin" method="POST")
    .form-block
      label(for="admin") Enter admin password HINT: ------
      input(id="admin" name="admin" type="password" required)
    button.submit Submit
  if message
    p.error #{message}
